check50:
  checks:
    hello world:
    - run: python3 hello.py # run python3 hello.py
      stdout: Hello, world! # expect Hello, world! in stdout
      stdin: "bruce"
      stdout: "bruce"
      stdin: "joe"
      stdout: "joe"
      exit: 0
